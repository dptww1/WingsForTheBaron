<!DOCTYPE html>
%html
%head
  - if content_for? :head
    = content_for :head
  - else
    %title Wings for the Baron Online
  = stylesheet_link_tag    "application", :media => "all"
  = javascript_include_tag "application"
  = csrf_meta_tags
%body

= yield
